name: Build GPUImage AAR

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Grant Permission
        run: chmod +x ./gradlew

      - name: Build AAR Only
        run: ./gradlew :gpuimage:assembleRelease

      - name: Upload GPUImage AAR
        uses: actions/upload-artifact@v4
        with:
          name: gpuimage-aar
          path: gpuimage/build/outputs/aar/*.aar
